import type { HandlerResponse } from '@netlify/functions';
export interface SourceImageResult {
    response?: HandlerResponse;
    cacheKey?: string;
    responseEtag?: string;
    finalize: () => Promise<void>;
}
export interface SourceImageOptions {
    cacheDir: string;
    url: string;
    requestHeaders?: Record<string, string>;
    modifiers: string;
    isLocal?: boolean;
    requestEtag?: string;
}
export declare const CACHE_PRUNING_THRESHOLD: number;
export declare function loadSourceImage({ cacheDir, url, requestEtag, modifiers, isLocal, requestHeaders }: SourceImageOptions): Promise<SourceImageResult>;
